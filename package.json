{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "Complete MERN Task Manager Application",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run prod --prefix backend\" \"npm start --prefix frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "test:roles": "node test-runner.js",
    "test:backend": "npm test --prefix backend",
    "test:frontend": "npm test --prefix frontend",
    "test:integration": "npm run test:backend -- --grep \"Integration\"",
    "test:all": "npm run test:backend && npm run test:frontend"
  },
  "devDependencies": {
    "chai": "^5.1.2",
    "concurrently": "^7.0.0",
    "mocha": "^11.0.1",
    "sinon": "^21.0.0"
  },
  "author": "Task Manager Team",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^6.18.0"
  }
}
