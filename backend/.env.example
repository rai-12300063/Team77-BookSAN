# MongoDB Atlas Connection String
# Replace with your own MongoDB URI
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/olpt?retryWrites=true&w=majority&appName=Cluster0

# JWT Secret Key for authentication
# Generate a secure random string for production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Port
PORT=5001

# Example MongoDB Local Connection (uncomment if using local MongoDB)
# MONGO_URI=mongodb://localhost:27017/olpt

# Note: Never commit the actual .env file to version control
# Copy this file to .env and replace with your actual values